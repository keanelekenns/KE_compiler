
int increment(int x){
    y;
    return x + 1;
}

int precedence_fun(int x, int y, int z){
    int[1] arr;
    arr[0] = 3;
    return x - 6 + y*increment(x)*2 - (y < z == false) ;
}

void main(){
    precedence_fun(1,2,3);
}