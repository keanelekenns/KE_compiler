void main() {
    boolean bool;
    int a;
    int b;

    // 3.1.1
    if (true) {
        print "true if statement";
    }

    // 3.1.2
    bool = true;
    while (bool) {
        print "looping while once";
        bool = false;
    }

    // 3.1.3 & 3.1.4
    print 5;
    println "new line";

    // 3.1.6
    b = a;
}

// 3.1.5
int foo() {
    int a;
    a = 5;
    return a;
}